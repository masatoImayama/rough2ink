# 漫画作画支援モデル 設計整理

作成日: 2026-09-03

---

## 1. プロジェクトの前提

### 目的

作家自身の原稿を学習データとし、ラフから作画工程を出力する支援モデルを構築する。**学習データが許諾済みであること**が事業上の必須要件であり、これが謳えない構成は採用しない。

### 需要の実態（作家ヒアリングより）

- メインのキャラ作画は作家自身が行う
- 生成に頼りたい工程の優先順位: **背景・小物 > モブ > トーン・ベタなどの仕上げ**
- 効果線はクリスタのツールで足りるため需要なし
- ラフから「線画」「線画+ベタ」「線画+ベタ+トーン」と**出力段階を選択できること**が重要

### 競合は「アシスタント人件費」ではなく「人員手配の不確実性」

300枚以上の原稿ストックがある作家でも、常時アシスタントを確保するのは困難。締切駆動の業務では平均コストより **可用性（必要なときに確実にある）** の価値が高い。

さらに、新規アシスタントは絵柄習得に数週間かかるのに対し、自分の原稿から学習したモデルは**立ち上がりが最初から完了している**。訴求はここに置く。

---

## 2. 事業モデル

### ① 個人作家の専用モデル

| 項目 | 内容 |
|---|---|
| 収益 | 初期学習フィー（10〜20万円）＋ 推論・再学習の継続課金 |
| 原価 | GPU 約1万円 + 前処理の人手 |
| 差別化 | モデルではなく**前処理パイプライン**（レイヤー抽出・正規化・分解） |
| 訴求 | 絵柄が外に出ない／オンボーディング済みアシスタント／即応性 |

**成立根拠**: 作家1人あたりの学習コストが1万円前後。「1人のために1モデル」が採算に乗る稀な領域。

**課金設計**: 収入が不安定な作家層には月額よりも「学習フィー＋ローカル実行の買い切り」が受け入れられやすい。再学習（新刊分の追加）を有償更新として継続収益化する。

**実行形態**: ブラウザにアップロードして待つUIは可用性訴求と矛盾する。**クリスタのプラグイン／自動アクションでローカル完結**を目指す。学習のみクラウド、推論はローカルGPU。

### ② 作家別データセットのカタログ化

寄与度ベースのインセンティブは採用しない。理由は以下。

- 拡散モデルのデータ寄与度推定（TRAK / D-TRAK / 影響関数）は精度が不十分で、「その数字が正しい」ことを証明できない
- 条件付き生成では出力の大部分をユーザー自身のラフが決めるため、個別提供者の限界寄与がノイズに埋もれる
- 少額配分は「私の30ページが月87円」という炎上リスクを生む

代わりに **データセット単位のライセンス取引** とする。

| 項目 | 内容 |
|---|---|
| 支払根拠 | ビルドマニフェストに記録された「使用したか否か」の離散的事実 |
| 配分 | モデル構築1件の売上の一定割合（例: 30%）を使用データセット提供者に配分 |
| 想定額 | 1データセットあたり1.5〜2万円/件。年20件使用で30〜40万円 |
| 供給の立ち上げ | ①の顧客は前処理済みデータセットを保有済み。納品時のオプトインでコールドスタートを回避 |

**設計上の必須条件**

1. **自作データの支配的地位を保証する** — ライセンスデータは事前学習まで、最終ファインチューニングは自分のデータのみ。この制約がないと②が①を食う
2. **重み納品の扱いを分岐** — 自前データのみのモデルは重み納品可、ライセンスデータを含むモデルは再配布禁止条項付きまたは管理ランタイム上でのみ実行。価格差の根拠にもなるが、**顧客への初回説明に含める**
3. **ビルドマニフェストの提供者公開** — いつ・誰に・いくらで使われたかが見える状態が事業の信頼の基礎

### 利用範囲の設計

「自身の原稿制作に限定」は境界が曖昧になる（原稿が後からグッズ化・単行本化されるため）。判定可能な形に書き換える。

- 生成物を最終作品に統合した形での利用は無制限（グッズ化・商業出版・配信を含む）
- 生成物を**単体で、または素材集・アセットとして頒布・販売することを禁止**
- 第三者からの受注制作（コミッション）における使用を禁止

**明示すべき点**: 禁止しているのは「モデルの用途」であって「成果物の権利」ではない。モデル出力に手を入れた時点でそれは作家自身の著作物。この区別を明示しないと、作家は「自分の原稿の権利が制限される」と読む。

### 提供データへのAI生成物混入の防止

**事業の存続に直結する最重要リスク。**

②が回り始めると、モデル出力を含む原稿が提供データとして戻る経路が構造的に発生する。被害は二重。

1. **品質の崩壊** — 自己出力の再学習で誤差が増幅する。線の癖が強調され、分解器の誤りが正解として固定される。数世代で崩れるが、劣化がゆっくり進むため気づいた時点で原因の切り分けが困難
2. **由来の連鎖の切断** — 他社の無断学習モデルの出力が混入した時点で「許諾済みデータのみ」が言えなくなる。事業の前提そのものが失われる

#### 規約での扱い

「法的措置を取る」という記載**だけ**では機能しない。理由は二つ。

- 混入の多くは**故意ではない**（背景の一部に使用、素材サイトのDL画像に混在、共同作業者の使用）。悪意のない提供者への威嚇は提供者数を減らすだけ
- **立証が困難**。事後に混入を証明するのは技術的に難しく、訴訟を実際に起こせる場面は限られる。実行できない威嚇は、一度見透かされると規約全体の信頼性を下げる

罰則の重さではなく、**責任の所在と金銭的帰結の明確化**に寄せる。

- 提供者は、提供データにAI生成物（当サービスの出力を含む）が含まれないことを**表明保証**する
- 混入が判明した場合、当該データセットを即時カタログから削除し、それを含むモデルの新規発行を停止する
- **再学習に要した費用を提供者に請求できる**旨を明記する
- 故意または重大な過失による場合は法的措置を留保する

抽象的な「法的措置」より、具体的な金銭的帰結のほうが抑止力がある。再学習費用は実際に発生する損害であり請求根拠も明確。

#### 検出側の手当て（規約と並行して実装）

| 手法 | 有効性 | 備考 |
|---|---|---|
| **自社出力へのマーキング** | 確実 | 生成時にステガノグラフィで不可視の識別子を埋める。**最優先で実装** |
| C2PA / コンテンツクレデンシャル | 部分的 | Adobe系ツールの出力にメタデータ。剥がされることも多いがゼロコストで拾える |
| 線の統計的特徴 | 補助的 | AI生成線は筆圧変化・入り抜きの分布が人間と異なる傾向。疑わしいデータのフラグ立てに使用 |
| 提供時の申告 | 運用で効く | 「制作にAIツールを使用したか」の項目。部分使用なら該当箇所を除外して受け入れる運用のほうが正直な申告を引き出せる |

**自社出力へのマーキングが最も費用対効果が高い。** ①の顧客の出力が②のカタログに戻る経路が最大のリスクであり、閉じた系の中で確実に効く。実装も軽い。

### リスク管理

免責条項は書くべきだが、**契約の外にいる第三者には対抗できない**。責任がないことと訴えられないことは別で、応訴コストは発生する。条項ではなく設計で手当てする。

- 提供者からの表明保証（連載作品の場合、出版社との契約確認のチェック項目を設置）
- 顧客ごとのモデル分離（被害を1顧客に封じ込める）
- ビルドマニフェストによる追跡可能性
- E&O保険（責任を負うためでなく、負っていないことを速く安く証明するため）

**法務確認事項**: 提供者規約と利用規約の背中合わせの整合（提供者に約束した制限が利用者側契約に漏れなく降りているか）。知財に強い弁護士に一度通す。

---

## 3. 技術設計

### 基本方針

| 決定事項 | 理由 |
|---|---|
| ベースモデルは使わず**フルスクラッチ** | ミツア系は非営利ライセンスで企業商用に制約。由来の連鎖を自前で完結させる |
| **事前学習済み重みを一切持ち込まない** | VGG perceptual loss / LPIPS / BLIP / CLIPスコアは全て使用不可。コードレベルで制約を固定 |
| **VAEを使わずピクセル空間で扱う** | 潜在空間の8倍ダウンサンプルは1〜2pxの線とトーン網点を破壊する |
| 拡散モデルではなく**決定的モデルから始める** | ペン入れ・ベタは出力の多様性が邪魔。U-Net + PatchGAN |
| **パッチ学習を基本**、背景のみ大域構造を扱う | 局所変換タスクにはパッチが有効。パースは大域問題 |

事前学習がどうしても必要な場合は、**数式生成のフラクタル画像**を使う（著作物が一切介在しない）。

### カスケード構成

```
ラフ ──→ 線画 ──→ +ベタ ──→ +トーン
        [段1]     [段2]      [段3]
```

独立した3モデルを直列に繋ぐ。利点:

- 途中で止められる（出力選択肢がそのまま実現）
- **途中を修正して続きから再実行できる**（線画を直してベタ段だけ回す）
- 各段の入出力ペアが同じソースから取れる

**出力は必ずレイヤー分離で返す。** 統合済み画像で返した時点で実用性が消える。

### アーキテクチャ

| 段 | 手法 | 損失 | 必要データ量の目安 |
|---|---|---|---|
| ベタ | セグメンテーション寄りU-Net | BCE + Dice | 50枚程度で動く可能性 |
| トーン | U-Net + 領域分類 | CE + 領域一貫性項 | 中規模 |
| 線画 | U-Net + PatchGAN | L1 + adversarial | 300枚 |
| 背景 | 低解像度大域ブランチ + 全解像度局所ブランチ | 同上 | 300枚では要検証 |

判別器も自分の線画のみで学習するため由来はクリーン。

### コマ単位の扱い

**前処理としては全タスクで必須。** パッチがコマ境界を跨ぐと無関係な2シーンが1サンプルに混ざり、学習不能な入力になる。クロップをコマ内に制約するだけで有効サンプルの質が上がる。

**学習単位としての有効性はタスク依存。**

- ベタ: ほぼ効かない（局所判断）
- トーン: 中程度（番号選択と濃度バランスはコマ全体で決まる）
- 背景・パース: 決定的（消失点の整合には大域視野が必須）

**縦横比の扱い**

リサイズ禁止。線幅は画風の一部であり、コマごとに異なる倍率で拡縮すると線幅の一貫性が学習できなくなる。

→ **画素スケール固定、キャンバスサイズのみ可変**。実装は以下のいずれか。

1. アスペクト比バケット化（効率は良いがサンプル数が少ないとバケットが痩せる）
2. **パディング + ロスマスク**（バッチサイズ1 + 勾配累積で十分。この規模ではスループットがボトルネックにならないため推奨）

**サンプル数の問題**: 300ページ × 約6コマ = 約1800コマ。パッチの数十万から激減するため、コマ単位のみでは過学習する。→ 大域ブランチは容量を小さく設計し、線の質感は局所ブランチ（数十万パッチ）に担当させる。

### 背景・パースの扱い

3D素材配置を前提とする案は**却下**（作業手数が減らず、それなら3Dを直接使うほうが早い。可用性訴求と矛盾する）。

代替の3方向:

1. **ラフに含まれるパース情報を捨てない** — 作家がアタリのパース線を引いている場合、それを条件として渡す。追加作業ゼロ
2. **背景モデルのみ二段構成** — 低解像度でコマ全体を見て構造を決め、高解像度パッチで線質を詰める
3. **精度要求を下げる** — 厳密なパース整合が要るのは見せ場の大ゴマのみで、そこは作家が描く。日常シーンの中景・小物に限定すれば破綻が許容範囲に収まる可能性

**「300枚では背景は無理」は未検証の思い込み。** どの程度破綻すると使い物にならないかを実測してから判断する。

### フキダシのノイズ対策

フキダシを作画の一部として描く作家が多く、機械的除去は困難。

**除去せず、損失マスクで無視する。** 穴埋めは偽の教師信号になるが、マスクなら何も学習しない。コマ外パディングのマスクと同じ仕組みに乗る（チャンネルを1つ増やすだけ）。

**マスクは過剰に広く取ってよい。** 誤検出のコストが非対称（失うのは学習サンプルの一部のみ）。フキダシの上下は情報密度が低い領域であることが多い。

検出の手がかり（単独では不十分、重ねて使う）:

- テキストレイヤーの矩形を膨張させた領域
- 一定面積以上で内部がほぼ白の連結領域（フチなしフキダシも拾える）
- 楕円・角丸の凸形状（フラッシュ型は別扱い）

代替案として、**フキダシ位置をユーザーが指定してモデルに条件入力する**設計もある。ノイズ問題が仕様に変わり、実作業手順とも一致する。

### 分解器（平坦データからの教師生成）

取り込み形式を画像・PDF・クリスタファイルの3系統に広げる場合、**平坦化された原稿からはペアが取れない**。これを解くのが分解器。

```
完成コマ画像 ──→ [分解器] ──→ (線, ベタ, トーン)
```

- トーン: 網点は周期構造 → 周波数解析で検出
- ベタ: 面積閾値以上の連結黒領域
- 線: 残り

**レイヤーが残っている300枚の位置づけが変わる。** 学習データとして直接使うのではなく、**分解器の教師データ兼検証データ**として使う。分解の正解が既知の唯一のサンプルであるため。

この構造の利点:

- レイヤーの有無が「データの有無」ではなく「精度の高低」の差になる
- ②のカタログで提供者に.clipのレイヤー整理を要求せずに済む。**PDFさえあれば参加できる**
- 分解誤差の下流伝播が弱点だが、レイヤー付き300枚で誤差を実測できるため定量判断が可能

---

## 4. 前処理の検証事項

着手前に実データで確認すべき項目。**すべて数枚〜数十枚の手検証で足りる。**

### A. 素材の実態確認

- [ ] 300枚のレイヤーデータは実際に残っているか（.clip / .psd）
- [ ] レイヤー命名規則は一貫しているか、原稿によってバラバラか
- [ ] 下書きレイヤーは生きているか（実ペアが何組取れるか）
- [ ] 背景ラフのアタリ線は独立レイヤーか、下書きに混在しているか
- [ ] 背景線画の「入力側」（ラフ・アタリ）が残っているか
- [ ] キャラと背景のレイヤー分離はどの程度徹底されているか
  - → **実質分離できないと想定**。データセットの粒度をレイヤー以外の軸で切る必要
- [ ] テキストレイヤーは機械的に分離できるか
- [ ] フキダシは作画レイヤーに含まれるか、独立しているか

### B. 分解器の成立性（最重要）

分解器の精度がこの設計全体を規定する。以下で破綻しないかを確認。

- [ ] **線の上にトーンを重ねる使い方**の頻度（線をトーンで潰す、線画レイヤーの上に乗せる）
  - 多い場合、分解難易度が一段上がる
- [ ] グラデーショントーンの検出可否
- [ ] テクスチャとして使用しているトーンの扱い
- [ ] キャラの主線とトーンが重なる箇所の分離精度
- [ ] ベタとトーン濃度の高い領域の判別
- [ ] レイヤー付き300枚に対する分解誤差の定量測定

### C. コマ分割の例外処理

コマ分割自体は古典的画像処理で対応可能だが、**例外処理が工数の大半を占める**。

- [ ] **断ち切りコマ** — ページ端で枠線が途切れ閉領域検出が失敗。仮想枠での補完が必要
- [ ] **キャラのはみ出し** — 矩形クロップだと隣コマを巻き込む。**ポリゴンマスクで切る**
- [ ] **斜めコマ・変形コマ** — 同じくポリゴン処理
- [ ] **見開き** — PDFでは1ページ結合の場合と分割の場合がある。判定処理が必要
- [ ] **効果線・集中線のコマ** — 枠線検出を誤らせる
- [ ] 枠線が独立レイヤーとして取れるか（取れれば矩形抽出だけで済む）

### D. 取り込み形式ごとの品質ゲート

②のカタログでは提供データの品質がそのまま商品価値になるため、入口で厳しく判定する。

| 形式 | 確認項目 | 失格条件 |
|---|---|---|
| 画像 | 短辺画素数、二値/グレー、圧縮痕 | web用縮小（網点がモアレ化） |
| PDF | 印刷入稿用か配信用か、JPEG圧縮率 | 配信用PDF（低解像度＋JPEG圧縮で線エッジ消失） |
| .clip | レイヤー取得可否 | 取得不可でも平坦化して他経路に流せる |

**自動判定の指標**: 短辺画素数 / 二値かグレースケールか / JPEG由来ブロックノイズの有無。閾値未満は弾く。

原稿解像度は600dpi・B4なら5000px級が前提。**JPEG圧縮痕は線画にとって致命的**。

---

## 5. モデル構築手順

### 着手順の原則

需要順（背景 > モブ > 仕上げ）と難易度順は**逆**になる。それでも **ベタ → トーン → 背景** の順で着手する。

理由は「背景が難しいから後回し」ではなく、**背景の実現可能性を判断するには実物で試すしかなく、そのためにデータパイプラインが先に必要**であるため。ベタで配管を全部通しておけば、背景モデルは差し替えるだけで載る。

いきなり背景から入ると、最難の問題と最面倒な配管を同時に解くことになり、高確率で停滞する。

### Phase 0: 素材検証（GPU不要）

1. 10枚程度で.clipのレイヤー別PNG書き出しをバッチ化し、**書き出し規則を確定する**
2. 上記「4. 前処理の検証事項」A〜Dを実データで確認
3. 分解器のプロトタイプを古典的画像処理で作り、レイヤー付きデータに対する誤差を測定
4. コマ分割のプロトタイプを作り、例外ケースの発生率を把握

**このフェーズで設計が変わる可能性がある。GPUを借りるのはコードが動いてから。**

### Phase 1: 配管の構築（ベタ段）

対象を**ベタ**に限定する。実質セグメンテーション問題で正解が一意に近く、目視評価が即座にできるため、配管の検証に最適。

構築する要素:

1. 取り込み（画像 / PDF / .clip の3系統）
2. コマ分割（ポリゴンマスク対応）
3. フキダシ検出 → 損失マスク生成
4. 分解器 → 教師データ生成
5. パッチ抽出（コマ内制約、スケール固定）
6. 学習ループ（U-Net + BCE/Dice）
7. **レイヤー分離での出力返却**
8. クリスタ連携（プラグイン / 自動アクション）
9. ビルドマニフェスト記録（②を見据えて最初から実装）
10. **出力へのステガノグラフィ・マーキング**（②を見据えて最初から実装。後付けすると、それ以前の出力が検出不能な状態で残る）

評価用に**30枚をホールドアウト**する。

### Phase 2: トーン段

配管はPhase 1で完成しているため、モデルの差し替えが中心。

- コマ単位の学習ループを追加（トーン番号選択と濃度バランスに大域視野が必要）
- 領域一貫性の損失項を検討
- カスケードの受け渡し（線画 → ベタ → トーン）を接続

### Phase 3: 背景段（実現可能性の検証を含む）

1. 300枚から背景ペアを抽出し、**まず小さく試して破綻の度合いを実測する**
2. 破綻が許容範囲 → そのまま進める
3. 破綻が大きい → 以下の順で対処
   - ラフのアタリ線を条件入力に追加
   - 大域ブランチ + 局所ブランチの二段構成に変更
   - 用途を日常シーンの中景・小物に限定

### Phase 4: ラフ → 線画（最終段）

**注意**: ネームからの線画生成と下書きからのペン入れは別タスク。ネームは構図の指示にすぎず、そこから線画を起こすのは解釈と創作であり、300枚では届かない。**下書き（鉛筆・青ラフ）→ ペン入れ**に限定する。

- 実ペア（実際の下書きスキャン + 完成線画）を数百組確保 → 評価・仕上げ用
- 合成ペア（完成線画を劣化させて逆生成）で数千〜万オーダーに水増し
  - 線の細線化、ストロークの揺らぎ付与、ランダムな途切れ、鉛筆質感の重畳
- **合成のみで学習すると「劣化を元に戻すモデル」になる。** 合成で事前学習 → 実ペアで微調整の二段が必須

### Phase 5: ②のカタログ化

Phase 1〜4で①が回り始めてから着手。②単体での立ち上げは供給が集まらず成立しにくい。

- 提供者規約・利用規約の整備（法務確認）
- **AI生成物混入の禁止条項と表明保証**、および検出フローの運用開始
- ビルドマニフェストの提供者向け公開UI
- 事前学習/ファインチューニングの分離制約を仕様として固定
- 支払・源泉徴収の実務フロー

---

## 6. インフラとコスト

### 必要スペック

512pxパッチのU-Net + PatchGANならVRAM 24GBで余裕。**RTX 4090一択**。H100 / A100は完全に無駄。

- フル学習1本: 20〜40時間
- ハイパラ探索・失敗込みの通算: 200〜300 GPU時間

### 価格（2026年時点、要再確認）

| プロバイダ | RTX 4090 | 備考 |
|---|---|---|
| Vast.ai | $0.29〜0.31/時（割り込み可）<br>$0.35〜0.50/時（オンデマンド） | マーケットプレイス。最安 |
| RunPod Community | $0.34/時 | 固定価格、標準コンテナ |
| RunPod Secure | $0.69/時 | SLA・データセンター保証 |
| さくら 高火力 DOK | 0.016円/秒（57.6円/時）〜 | 国内DC、秒課金、最低利用時間あり |

**250時間 × $0.34 ≈ $85（1万数千円）。GPU代は誤差。**

### 運用方針

1. **前処理はローカルで完結** — レイヤー分離・位置合わせ・正規化はCPU作業。GPU時間課金でやるのは無駄
2. **アップロードはページ単位、クロップはDataLoader内で** — 600dpiページペアで10〜20GB程度。事前パッチ展開は数十GBに膨れる
3. **割り込み可インスタンス + 1000ステップごとのチェックポイント**
4. **探索と本番で環境を分ける** — Vast.aiの非検証ホストは他人のマシン。未発表原稿を置かない。探索はダミーデータでVast.ai、本番学習はRunPod Secureかさくらに載せる

### 注意点

- さくらの最安帯はV100プラン。**2026年12月31日で新規受付終了、2027年3月31日でサービス終了**が告知済み。ここに環境を作り込まない
- Colabは数日規模の学習に不向き（セッション切断）
- ハイパースケーラー（AWS/GCP/Azure）は単価が数倍 + 下り転送料。この規模では選ぶ理由がない

---

## 7. 未決事項

| 項目 | 決定に必要なもの |
|---|---|
| 背景モデルの実現可能性 | Phase 3での実測 |
| 線画データセットのカタログ化範囲 | キャラ主線と背景線の分離が実質不可能である前提での再設計 |
| 分解器の精度が実用に足るか | Phase 0での誤差測定 |
| フキダシ検出の必要精度 | Phase 1で実際に破綻が起きるかの観察 |
| 提供者規約と利用規約の整合 | 知財に強い弁護士による確認 |
| 重み納品の分岐条件 | ②着手時点で確定、ただし①の顧客への初回説明に含める |
| 他社モデル出力の検出手段 | 自社出力はマーキングで閉じられるが、外部生成物の検出は決定打がない。運用（申告＋フラグ立て）でどこまで許容するかの方針決定 |

---

## 補記

本ドキュメントの法務に関する記述は一般的な整理であり、法的助言ではありません。事業化前に弁護士による確認を推奨します。
